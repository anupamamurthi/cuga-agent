# video_qa — app dependencies

# cuga framework
cuga
cuga-skills
langchain-core

# transcription — faster-whisper is recommended (~4x faster than openai-whisper)
# falls back to openai-whisper automatically if faster-whisper is not installed
faster-whisper
# openai-whisper  # uncomment as fallback if faster-whisper is unavailable

# vector index + semantic search
chromadb
sentence-transformers

# web UI (only needed for --web mode)
fastapi
uvicorn

# system dependency (not pip-installable):
#   brew install ffmpeg   ← required for video files (.mp4 .mov .mkv ...)
#   audio-only files (.wav .mp3 .m4a) skip ffmpeg entirely
