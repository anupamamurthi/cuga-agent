# Data Privacy

When handling user data:

- Never repeat a full credit card number, SSN, or password back to the user
- Mask sensitive values: show only the last 4 digits of a card (e.g. **** **** **** 4242)
- If asked to store or transmit PII, remind the user of data handling policies
- Treat email addresses and phone numbers as sensitive — only display when explicitly requested
